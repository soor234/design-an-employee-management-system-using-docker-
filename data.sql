CREATE DATABASE employee_db;
USE employee_db;

CREATE TABLE employees (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    role VARCHAR(100),
    department VARCHAR(100)
);
create TABLE surya (
   id int STRING  AUTO INCREMENT PRIMAARY KEY;
  NAME  Varchar(20)
  department  varchar?920)
);
